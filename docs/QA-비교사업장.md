# 품질확인서 — 비교사업장 탭

- 일자 : 2026-09-14
- 요구 : ① 반경 1/2/3km 설정 → 지도에 반경 + 반경내 아파트 리스트업
        ② 분양가를 전용면적 기준 ㎡당 평균으로 표시
        ③ 여러 단지 선택 → 분양가 산술평균 실시간 산출
        ④ 선택 단지의 주소·이름·면적별 세대수를 별도 그리드로 (공공데이터 API)

## 1. 분석 — 분양가를 주는 원천이 있는가

| 후보 | 판정 |
|---|---|
| 국토부 아파트 실거래 | **분양가 아님** — 이미 팔린 값 |
| KB 시세 | **분양가 아님** — 기축 아파트 시세 |
| **한국부동산원 청약홈 분양정보** (공공데이터포털) | **채택** — 주택형별 분양가를 준다 |

키는 이미 있는 `DATA_GO_KR_KEY` 를 쓴다. 다만 포털 키는 **API 마다 따로 활용신청**이 필요하므로
"키가 있다" 와 "이 API 가 된다" 는 다른 이야기다 → 탐색 창구 `/api/applyhome` 을 먼저 배포해 확인했다.

**실측 결과 (2026-09-14, 둘 다 현재 키로 열림)**

```
분양정보  전국 2,875건 · 경기 913건 · perPage 1000 수용
주택형별  HOUSE_TY "084.7459A"      → 전용면적 84.7459㎡ + 타입기호
          SUPLY_AR "110.3043"      → 공급면적 (전용 아님)
          LTTOT_TOP_AMOUNT "89800" → 8억 9,800만원 (만원 단위)
          SPSPLY 16 + SUPLY 11     → 27세대
```

> **함정 — `SUPLY_AR` 을 전용면적으로 쓰면 단가가 25% 낮게 나온다.**
> 이름이 "공급면적"이 아니라 "SUPLY_AR" 이라 전용으로 읽기 쉽다.
> 전용률 76.8%(84.75/110.30)로 교차확인해 `HOUSE_TY` 가 전용임을 확정했다.

**또 하나 — 청약홈도 행정구역이 옛 체계다.** 광주 36건 / 전남 24건이 따로 잡힌다(실측).
KOSIS 와 같은 함정이라 `noticeSido()` 로 시군구를 봐서 가른다.

## 2. 설계

- **2단 필터로 지오코딩을 줄인다.** 시도 공고를 전부 지오코딩하면 800건이라 느리고 쿼터도 아깝다.
  → 시군구 중심좌표로 1차(반경+25km) → 남은 것만 지번 조회.
  실측 832건 → 326건 → 반경 판정.
- **재공고를 묶는다.** 원공고·조합원취소분·잔여세대가 각각 올라오는데 표기가 다르다.
  공백 지운 단지명 + 읍면동으로 묶고 최신 공고만 남긴다.
- **단지 대표단가는 두 가지를 다 낸다.** 세대수 가중평균(분양수입에 가깝다)과 주택형 단순평균.
  실무 관행이 갈리므로 **고르게 하고 어느 쪽을 썼는지 화면·엑셀에 같이 적는다.**
  단지끼리는 요구대로 **산술평균**을 쓴다.
- 거리·반경은 다른 시트와 같은 규칙 — 경계 기준으로 그릴 때만 경계 최단거리로 잰다
  (그린 선이 곧 판정선).

## 3. 개발

| 파일 | 역할 |
|---|---|
| `src/collectors/applyhome.js` | 공고·주택형 수집, 주소 정규화, 재공고 묶기, 2단 필터, 단가 산출 |
| `app/api/applyhome/route.js` | 원천 탐색 창구 (키가 배포 환경에만 있다) |
| `app/api/apts/route.js` | 비교사업장 수집 (`probe=` 로 진단 가능) |
| `app/CompareView.js` | 탭 화면 — 반경 선택 · 목록 · 실시간 평균 · 지도 · 상세 그리드 |
| `app/exportExcel.js` | 비교사업장 시트 (목록 + 선택 상세 + 반경지도) |
| `app/storage.js` · `app/page.js` | 고른 단지까지 보관해야 평균이 재현된다 |

## 4. 검증

### 4-1. 원천 (배포본 실호출, 골든 사업장 좌표)

사업지 : 경기도 광주시 탄벌동 203-4 (127.243983, 37.421562)

| 반경 | 결과 |
|---|---|
| 1km | 0건 — 반경 안에 분양 이력 없음 (화면에 "반경을 넓혀 보세요") |
| 2km | 3건 |
| 3km | **5건** (중복 정리 전 9건 → 재공고 묶어 5건) |

```
1294m  광주 탄벌 서희스타힐스 2단지   2025-06-26   34세대   8,144,483 원/㎡
1327m  광주 탄벌 서희스타힐스 1단지   2025-06-26   32세대   8,032,341
1405m  광주 송정 중흥S-클래스 파크뷰  2022-12-02  840세대   6,812,758
2190m  광주 더파크 비스타 데시앙      2022-10-21 1,690세대  6,939,450
2235m  해링턴 스퀘어 리버파크         2025-06-20  817세대   9,407,648
```

응답 4.9초(첫 호출) · 1.3초(캐시 후).

**누락 점검** — "바로 옆 탄벌동 서희스타힐스가 왜 안 나오나" 를 진단창구(`probe=`)로 되짚어
지오코딩·거리까지 확인했다. 실제 1,294m 로 1km 밖이었고 2km 부터 들어온다(누락 아님).

### 4-2. 화면 (로컬 Chromium, 배포본 응답을 그대로 물려 로직만 검증)

```
목록표 9열 · 5행 · 체크박스 5개
선택 0곳 → 산술평균 —
선택 1곳 → 8,144,483 원/㎡ (평당 약 26,923,910)
선택 2곳 → 7,478,621 원/㎡     기대 (8,144,483 + 6,812,758)/2 = 7,478,621   ✓ 일치
[단순평균] 으로 전환 → 7,493,678   기대 (8,182,150 + 6,805,207)/2 = 7,493,678  ✓ 일치
```

선택 단지 상세(면적별) 그리드 :

```
["광주 탄벌 서희스타힐스 2단지","경기도 광주시 탄벌동 532-2번지 일원(탄벌4지구 A2블럭)","059.9681A","59.97","9","495,000,000","8,254,389"]
["059.9807C","59.98","18","483,000,000","8,052,590"]
["059.9553E","59.96","7","494,000,000","8,239,472"]
["광주 탄벌 서희스타힐스 2단지 계 · 세대수 가중평균","34","-","8,144,483"]
```

단가 교차검산 : 495,000,000 ÷ 59.9681 = **8,254,389 원/㎡** ✓
가중평균 : (8,254,389×9 + 8,052,590×18 + 8,239,472×7) ÷ 34 = **8,144,483** ✓

> 로컬에서는 카카오 JS 키가 도메인 제한이라 지도가 안 뜬다(기존에 기록된 제약).
> 지도는 다른 시트와 **같은 `RadiusMap` 컴포넌트**를 쓰며, 새로 들어간 것은 마커 배열뿐이다.

### 4-3. 컴파일 / 배포

`npx next build` 통과 · 배포 커밋 확인.

## 5. 요구사항 적합 여부

| 요구 | 판정 | 비고 |
|---|---|---|
| ① 반경 1/2/3km 설정 · 지도에 반경 · 반경내 아파트 리스트업 | 충족 | 핀 번호 = 표의 # |
| ② 전용면적 기준 ㎡당 분양가 표시 | 충족 | 평당 환산도 같이 표기 |
| ③ 여러 단지 선택 → 산술평균 실시간 | 충족 | 실측 두 경우 모두 기대값과 일치 |
| ④ 선택 단지 정보를 별도 그리드로 (공공데이터 API) | 충족 | 주소·단지명·주택형·전용면적·세대수·분양최고금액·단가 |
| 엑셀 내보내기 | 충족 | 「비교사업장」 시트 — 목록 · 선택 상세 · 반경지도 |
| 보관/복원 | 충족 | 고른 단지까지 보관 |

## 6. 한계 · 남은 것

- **분양 이력이 있는 단지만 나온다.** 청약홈에 공고가 올라온 분양 단지가 원천이므로
  기축 아파트나 공고 이전 단지는 대상이 아니다. (비교사업장 = 분양가 비교이므로 의도한 범위)
- **분양최고금액 기준이다.** 층·향별 차등의 최고가라 실제 평균 분양가보다 소폭 높을 수 있다.
  청약홈이 공개하는 값이 이것뿐이다.
- 공고일이 오래된 단지(2022년 등)도 반경 안에 있으면 표에 남는다 — **공고일을 표에 같이 적어**
  실무자가 시점을 보고 고르게 했다. 시점 필터가 필요하면 `/api/apts&from=YYYY-MM-DD` 가 이미 있다.
- 지도 렌더는 배포본에서만 확인 가능(로컬은 카카오 JS 키 도메인 제한).
