# PF 보증심사 통계 자동수집 — 유지보수 노트

> 새 세션에서 이 파일을 먼저 읽을 것. 삽질을 반복하지 않기 위한 기록이다.

## 무엇을 만드는가

사업장 시군구를 넣으면 PF 보증심사에 필요한 **통계 수치와 증빙(캡쳐)** 을 원천에서 자동 수집한다.
실무자는 결과를 엑셀 평가표에 붙여넣는다. **심사 자체는 내부망에서 한다** — 이 앱은 자료수집 보조 도구다.

- 배포: https://pfstatistics.vercel.app/ (Vercel, 서울 리전 icn1)
- 브랜치: **`main` 에 직접 커밋·푸시** (사용자 승인 완료). 푸시하면 자동 배포된다.
- **사용자는 터미널을 쓰지 않는다.** 모든 조작은 웹앱이나 각 서비스 콘솔에서 되어야 한다.
  `npm run ...` 을 시키지 말 것.

## 골든 데이터셋 (가장 중요한 자산)

`docs/evidence-samples/` 의 캡쳐 11장은 참고자료가 아니라 **정답지**다.
표본: **경기도 광주시 탄벌동 203-4 외 57필지 / 시공사 제일건설(주)**

| 지표 | 시점 | 정답 |
|---|---|---|
| 미분양주택수 | 202607 | 93 호 |
| 주민등록세대수 | 202607 | 178,187 세대 |
| 주택보급률(경기) | 2024 | 99.4 % |
| 소비심리지수(경기도) | 202607 | 125.1 |
| CD(91일) | 최신 | 3.12 % |
| KB 매매지수 증감률 | 202608 | 0.341 % |
| 시공능력평가순위 | 2025 | 17 위 |
| 의료시설(병원급, 1.5km) | - | 0 곳 부재 (의원급 22곳 제외) |

**CD(91일)은 매 영업일 변한다.** 고정 정답 대조 대상이 아니며 자가진단은 범위(0.5~10%)로 감시한다.
캡쳐의 3.12 는 2026-09-04 기준값일 뿐이다.

수집기를 고치면 반드시 이 값들이 재현되는지 확인한다. `/api/collect` 응답의 `golden.match` 로 자동 대조된다.

## 확정된 원천 파라미터 (다시 찾지 말 것)

### 키 불필요 — 내부 엔드포인트 역산
```
미분양주택수   GET stat.molit.go.kr/portal/stat/data.do
               ?formId=2082&styleNum=128&apprYn=Y&startDate=YYYYMM&endDate=YYYYMM
               formId 2082=시·군·구별 미분양현황 / 2080=규모별 / 5328=공사완료후 / 2086=종합
               응답 {"0":기준월,"1":시도,"2":시군구,"3":호수}  시도는 축약형("경기")

KB 매매지수    GET data-api.kbland.kr/bfmstat/weekMnthlyHuseTrnd/priceIndex
               ?매매전세코드=01&월간주간구분코드=01&메뉴코드=1&지역코드={시도코드}
               지역코드 없으면 시도 단위, 있으면 그 시도의 시군구 목록
               ※ 증감률 전용 엔드포인트 prcIndxFlxblRt 는 서버측 SQL 버그로 못 씀
                 → 지수 원본을 받아 (당월/전월-1)*100 로 직접 계산 (캡쳐와 소수3자리까지 일치)

CD(91일)       POST kofiabond.or.kr/proframeWeb/XMLSERVICES/  (Content-Type: application/xml)
               ProFrame XML: BIS-KOFIABOND / BISCDPSrchSO / selectCdpMain
               응답 val1=고시일 val2=오전 val3=오후
               ※ 기준일자 파라미터를 무시하고 최신 영업일만 준다 (과거 소급 미지원)
               화면경로는 메뉴서비스(BIS-COM/BISComMenuSO/selectMenuBIS, workGb=KOR)로 찾았다
```

### KOSIS (KOSIS_API_KEY 필요)
**itmId 와 objL1 을 둘 다 넘기지 않으면 통계표 ID 가 맞아도 `err 20` 이 난다.** 이것 때문에 오래 막혔다.
```
주민등록세대수  orgId=101 tblId=DT_1B040B3 itmId=T1        objL1=법정동코드앞5자리(광주시 41610) prdSe=M
주택보급률      orgId=116 tblId=DT_MLTM_2100
                itmId=13103871096T6 (보급률, 다가구 구분거처 반영)
                objL1=13102871096A.0011 (경기)  ← 일반 행정코드가 아니라 통계표 전용코드
소비심리지수    orgId=390 tblId=DT_39002_04 itmId=T1 objL1=K0205(경기도) prdSe=M
```
- 메타 조회는 `Param/statisticsParameterData.do` 가 아니라 **`statisticsData.do`** + `loadGubun`(ITM=1/OBJ=2/PRD=3)
- 탐색 창구: `/api/kosis?q=키워드` · `?orgId=&tblId=&meta=ITM` · `?orgId=&tblId=&period=&itmId=&objL1=`
  **키가 배포 환경에만 있으므로 통계표 탐색은 이 엔드포인트로 원격 수행한다.**

### 카카오 (KAKAO_REST_KEY / JS키)
```
지하철역 SW8 1km · 상업 MT1+"백화점"키워드 1.5km
문화 CT1 1km · 공공 PO3 1km · 공원 키워드"공원" 1km · 학교 SC4 500m/1km(이름필터)
6차선 왕복도로는 POI 가 아님 → 도로명주소 도로구간DB(차로수) 필요. 현재 수기입력.
```
**상호가 아니라 카카오 분류(category_name)로 걸러야 한다.** 상호로 거르면
"밧데리백화점"이 상업시설로, 동물병원이 의료시설로 잡힌다(실제 광주시 조회에서 확인).

### 의료시설 — 카카오 아님. 심평원 (DATA_GO_KR_KEY 필요)
카카오 카테고리 말단은 **진료과목**("의료,건강 > 병원 > 정형외과")이라 의료법상 종별을 못 가린다.
30병상 넘는 전문병원이 걸러지고 의원이 병원으로 잡힌다.
```
GET apis.data.go.kr/B551182/hospInfoServicev2/getHospBasisList
    ?serviceKey=&xPos=경도&yPos=위도&radius=미터(최대5000)&_type=json
```
**심사 기준은 병원급 이상**(사용자 확정). 의료법 제3조:
- 병원급 = 종합병원·병원·요양병원·정신병원·치과병원·한방병원·상급종합병원 (30병상 이상)
- 의원급 = 의원·치과의원·한의원 (30병상 미만) → 제외

판정은 **종별명(clCdNm)을 명시적으로 적어** 가른다.
처음엔 "병원으로 끝나면 병원급" 으로 뒀는데 **틀렸다** —
심평원 종별명은 "상급종합병원" 이 아니라 **"상급종합"** 이라 그 규칙으로는
전국 상급종합병원 47곳이 통째로 빠진다(실측). 가장 큰 병원들이 빠지는 셈이다.
```
병원급 : 상급종합 · 종합병원 · 병원 · 요양병원 · 정신병원 · 치과병원 · 한방병원
의원급 : 의원 · 치과의원 · 한의원 · 조산원          → 제외
지역보건법 기관 : 보건소 · 보건지소 · 보건진료소 · 보건의료원  → 제외
```
탐색 창구: **`/api/hira?lat=&lng=&radius=`** (종별 분포·totalCount 를 그대로 준다).
캡쳐가 1.5km "부재"인데 의원·치과가 15건 잡혔던 것이 이 기준의 근거다.

**배선**: `app/api/facilities/route.js` 가 카카오로 전 시설을 훑은 뒤
`facilities['의료시설']` 만 `collectMedical()`(심평원) 로 **덮어쓴다.**
카카오 `FACILITY_SPEC` 에는 의료시설 항목이 아예 없다(주석만 남아 있다).
실측(서울 대학로 1.5km): 서울대학교병원 **상급종합** · 서울대학교치과병원 치과병원 ·
리움한방병원 한방병원 · 포레스트병원 병원 = 4곳, 의원 95 · 치과의원 83 · 한의원 64 = 242곳 제외.
봉투의 `source.name` 이 「건강보험심사평가원 병원정보서비스」인지로 확인한다 —
카카오 봉투는 「카카오맵」이다.

### 분양가 — 청약홈 (DATA_GO_KR_KEY 필요)
비교사업장의 **분양가**를 주는 유일한 공공 원천이다. 실거래는 이미 팔린 값이고, KB시세는 기축이다.
```
공고   GET api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1/getAPTLttotPblancDetail
       ?page=&perPage=1000&cond[SUBSCRPT_AREA_CODE_NM::EQ]=경기
주택형 GET api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1/getAPTLttotPblancMdl
       ?cond[HOUSE_MANAGE_NO::EQ]=2026000449
```
**HOUSE_TY 가 전용면적, `SUPLY_AR` 이 공급면적이다** — `"084.7459A"` = 전용 84.7459㎡.
**심사가 쓰는 것은 공급면적 쪽이다**(평가표 검산). 전용으로 내면 약 30% 높게 나온다.
분양개시일은 모집공고일이 아니라 **`CNTRCT_CNCLS_BGNDE`(공급계약시작일)** 이다(실무 Q&A).
`LTTOT_TOP_AMOUNT`(89800)는 **만원** 단위. 세대수 = `SPSPLY_HSHLDCO`(특별) + `SUPLY_HSHLDCO`(일반).
`RENT_SECD_NM='분양주택'` 만 남긴다(임대는 분양가가 없다).
전국 2,875건 · 경기 913건 (2026-09 기준). perPage 1000 까지 받는다.
**적재 기간은 2020-02-05 ~ 2026-09-11 — 약 6.5년치다**(실측 2026-09-17).
전에 "2023-10-25 ~ · 약 3년치" 라고 적었던 것은 **틀렸다.**
규정 제16조는 "분양개시 후 10년 경과 제외" 라 0~10년이 대상인데 원천은 0~6.5년이다 —
**6.5~10년 구간(2016~2020 분양)이 통째로 빈다.** 그 구간의 기축단지는 어떤 조회로도 안 나온다.
**오피스텔·도시형생활주택·민간임대는 다른 API 에 있다** — APT 쪽만 보면 통째로 안 보인다.
```
GET api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1/getUrbtyOfctlLttotPblancDetail   (전국 619건)
GET api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1/getUrbtyOfctlLttotPblancMdl
```
필드 이름이 다르다: 전용면적 `EXCLUSE_AR` · 금액 `SUPLY_AMOUNT`(만원) · 타입 `TP`.
세부구분 = 오피스텔 442 · 도시형생활주택 94 · 민간임대 80 · 생활형숙박시설 3.
실측: 서울 신림동 반경 2km 8건 중 **5건이 오피스텔·도시형**이었다.

**민간임대의 `SUPLY_AMOUNT` 는 분양가가 아니라 임대보증금이다**(실측).
힐스테이트 용인포레(기업형민간임대) 59.96㎡ → 1.35억 → 2,080,944원/㎡.
같은 데이터셋 도시형생활주택(분양)은 14,723,136원/㎡ — 자릿수가 다르다.
그대로 섞으면 분양가 평균이 반토막 난다. 값은 보여주되 **선택을 막는다**.

**공고 주소는 지번이 없는 게 많다 — 점점 짧게 잘라 여러 번 묻는다**(`geocodeSupply`).
한 번만 물었을 때 경기 고유주소 945건 중 **46.7% 가 실패**했다. 단계는
`exact`(지번) → `name`(단지명 장소검색) → `zone`(택지지구) → `dong`(읍면동) → `place`.
근사인지 아닌지를 **봉투에 실어 끝까지 들고 다닌다**(화면 배지·지도 핀 이름).
실측 실패율(2026-09-17): 경기 0.7% · 인천 0.8% · 서울 0.7% · 부산 0.5% · 창원 0.7% · 천안 1.2% · 대구 3.1%.
남은 실패는 카카오에도 그 지구가 없는 것(당수1지구·가재지구)과 주소가 아닌 것("수도권 소재 국민임대주택")이다.
전수조사 창구: **`/api/apts?...&census=1`** · 카카오 탐색: **`/api/kakao?q=`·`?addr=1`(현재 행정구역)**

탐색 창구: **`/api/applyhome?op=detail|model|raw&path=`**, 수집: **`/api/apts?x=&y=&region=&radius=`**
(`&probe=정규식` 을 붙이면 반경 밖 공고까지 지오코딩 결과·거리를 보여준다).

### K-apt 공동주택 (DATA_GO_KR_KEY — 활용신청 완료, 실측 2026-09-17)
**준공되어 입주한 단지**만 있다(관리비 의무단지). 분양중·미착공은 없고 **분양가도 없다.**
그래서 비교사업장을 대체하지 못한다 — 좌표·세대수·시공사·사용승인일을 **보강**하는 데 쓴다.
```
단지목록  GET apis.data.go.kr/1613000/AptListService4/getSigunguAptList4?sigunguCode=41610
          getSidoAptList4 · getLegaldongAptList4(bjdCode) · getRoadnameAptList4 · getTotalAptList4
          → kaptCode · kaptName · bjdCode · as1~as4(시도/시군구/읍면동/리)   좌표·지번 없음
기본정보  GET apis.data.go.kr/1613000/AptBasisInfoServiceV5/getAphusBassInfoV5?kaptCode=A10023961
          → kaptAddr(**지번주소**) · doroJuso(도로명) · kaptUsedate(사용승인일)
            hoCnt(세대) · kaptBcompany(시공사) · kaptTopFloor · privArea …
```
**버전 숫자를 찍지 말 것** — `AptListService2/3` 도, `AptBasisInfoServiceV2/V3` 도 전부
`NO_OPENAPI_SERVICE_ERROR`(코드 12, "서비스가 없거나 폐기됨") 다. 키 문제로 오해하기 쉽다.
실제 경로는 포털 상세페이지 HTML 에서 찾았다 —
`www.data.go.kr/data/15057332/openapi.do`(단지목록) · `/data/15058453/`(기본정보) 를 받아
`apis\.data\.go\.kr/...` 와 `get[A-Z]...` 를 긁으면 나온다.
탐색 창구: **`/api/applyhome?op=raw&host=apis.data.go.kr&path=...`** (다른 포털 서비스도 이걸로 시험)

### 국토교통부 아파트 매매 실거래가 (DATA_GO_KR_KEY — 별도 신청 없이 됨, 실측 2026-09-17)
**기축 단지의 유일한 가격 원천.** 청약홈 적재가 2020-02 부터라 그 앞 분양 단지는 분양가가 어디에도 없다.
```
O  apis.data.go.kr/1613000/RTMSDataSvcAptTrade/getRTMSDataSvcAptTrade
     ?LAWD_CD=시군구5자리&DEAL_YMD=YYYYMM&numOfRows=1000&_type=json
X  .../RTMSDataSvcAptTradeDev/...   → SERVICE_KEY_IS_NOT_REGISTERED (미신청)
응답 aptNm · umdNm · jibun · excluUseAr(**전용면적**) · dealAmount "104,000"(**만원**) · dealYear/Month/Day · floor · buildYear
```
월·시군구 단위라 최근 12개월을 받아 단지별 **Σ금액 ÷ Σ전용면적**(면적 가중)으로 단가를 낸다.
**분양가가 아니다** — 이미 팔린 값이고 **공급면적을 안 준다**. 심사기준(공급면적) 단가와 그대로 비교하면 안 되고
비교사업장 평균에도 넣지 않는다. 화면·엑셀에 그 사실을 같이 적는다.

### HUG 지역 평균 분양가·초기분양률 — **KOSIS 로 받는다** (KOSIS_API_KEY, 실측 2026-09-17)
"분양가를 못 얻을 때는 분양보증 현황에서 가져오자"(사용자 제안).
**공공데이터포털의 HUG 오픈API 는 이 키로 안 열린다** — 분양이력정보(15057686)·분양보증현황(15011457)·
지역별 ㎡당 분양가격(15070256) 전부 odcloud `code -3 등록되지 않은 서비스`.
그런데 **같은 통계가 KOSIS 에 orgId=414 로 올라와 있고 KOSIS 키는 이미 있다.**
```
분양가   orgId=414 tblId=DT_41401N_005  월 2015.10~   단위 천원/㎡
         itmId=00 · objL1=지역(AA) · **objL2=규모(AB005)**
         objL2 를 빠뜨리면 itmId·objL1 이 맞아도 "해당 자료가 없습니다"
         지역 AA : 00전국 01서울 02인천 03경기 05부산 06대구 07광주 08대전 09울산 10세종
                   12강원 13충북 14충남 15전북 16전남 17경북 18경남 19제주
         규모 AB005 : T1전체 T2전용60이하 T3전용60~85 T4전용85~102 T5전용102초과
초기분양률 orgId=414 tblId=DT_41401N_008  분기 2015 3/4~  단위 %
         itmId=B · objL1=지역(AA)
         **단일 시점(start=end)은 err 30, 범위조회(start≠end)는 된다** — 주택보급률 표와 **정반대**다
```
실측(경기): 202607 평균 분양가 **7,192천원/㎡** · 2026 2분기 초기분양률 **80.8%**.
**시도 단위다** — 규정 제16조의 「인근 유사사업장 평균가격」을 대체하지 못한다.
비교사업장 평균에 섞지 말고 **기준선**으로만 쓴다.
초기분양률도 「인근 단지」가 아니라 지역 평균이라 **참고 표기로만 끝낸다** —
처음엔 [이 값 넣기] 버튼을 뒀는데 **넣을 이유가 없다**(사용자 지적 2026-09-17).
규정과 맞지 않는 값을 한 번의 클릭으로 칸에 앉힐 수 있게 두면 그게 실수의 통로가 된다.
(「인근아파트 초기 분양률(10)」 은 **본래 수기입력 항목**이다 — 옆 단지를 조사해 넣는다)
**면적 기준(공급/전용)을 원천이 명시하지 않는다** — 단정하지 말고 화면에 그대로 적는다.
(정황: 광주시 공급 5.77백만 < 경기 7.19백만 < 광주시 전용 7.70백만 — 공급 쪽이 일관되지만 실측은 아니다)
탐색 창구: **`/api/hug?region=`** · KOSIS 메타는 `/api/kosis?orgId=414&tblId=&meta=ITM`

### 시공능력평가순위
연 1회(8월) 공시라 API 불필요. **원본 공시를 통째로 적재**한다 (`tools/build-constructor-rank.mjs`).
```
대한건설협회 > 협회공시 > 공시자료  /lay1/bbs/S1T10C14/A/4/list.do
본문의 첨부 /download.do?uuid=....xlsx  ← 전 업체
시트는 업종별(토건·토목·건축·산설·조경) — 심사가 쓰는 것은 토건(토목건축공사업)
머리글 위치·이름이 연도마다 다르다: 2024 "연번/토건등록번호/토건시평" · 2025~ "순위/등록번호/시공능력평가액"
```
2024 2,960 · 2025 2,884 · 2026 2,784건. 명부가 1.7MB라 번들에 싣지 말고 **`/api/constructors`** 로 찾는다.
상호 표기차((주)/㈜/주식회사/공백)를 정규화해 매칭한다 — 공시 원본도 "현대건설(주)" 와
"주식회사 포스코이앤씨" 가 섞여 있다. **동명 업체가 있다**(제일건설(주) 17위 / (주)제일건설 529위).

### 행정구역은 실제로 바뀐다 (2026 기준)
- **전라남도 + 광주광역시 → 전남광주통합특별시**
  통계누리 "전남광주" · KB "광주(코드 12)" · 카카오 "전남광주통합특별시"(정식). 옛 것은 KB 에 "(구)광주광역시/(구)전라남도" 로 남아 있다.
  **KOSIS 두 통계표(주택보급률·소비심리)는 아직 광주/전남을 따로 집계한다** → 시군구로 가른다
  (옛 광주광역시 5개 구 = 동구·서구·남구·북구·광산구). `src/lib/sido.js` `statSido()`
  **청약홈 분양정보도 마찬가지다**(실측: 광주 36건 / 전남 24건이 따로 잡힌다).
  `src/collectors/applyhome.js` `noticeSido()` 가 같은 규칙으로 가른다.
- **인천 개편** — 중구·동구·서구 → 영종구·제물포구·서해구·검단구 등.
- `data/regions.json` 은 통계누리 최신월로 다시 생성한다(`tools/build-regions.mjs YYYYMM`).
  **낡으면 그 지역이 선택지에 아예 없다** ("검단구가 검색 안 된다" 가 그 증상이었다).

## 겪은 함정들

| 증상 | 진짜 원인 |
|---|---|
| KOSIS `err 20` | tblId 는 맞는데 **itmId/objL1 누락** |
| KOSIS `err 11` | 키 오탈자가 아니라 **오픈API 활용신청 미승인**. 승인되니 바로 풀렸다 |
| 카카오 `403 disabled OPEN_MAP_AND_LOCAL` | 키 문제 아님. 콘솔 **제품 설정 > 카카오맵 OFF**. REST·JS 가 한 서비스로 묶여있다 |
| 카카오 JS 키가 "없음" | 환경변수 이름 불일치(`KAKAO_JS_KEY` vs `NEXT_PUBLIC_...`). **`/api/config` 가 서버에서 내려주도록 해결** |
| curl 은 되는데 노드는 안 됨 (반대도) | 이 샌드박스의 curl 이 일부 국내 호스트에서 프록시 문제를 일으킨다. **판정은 Node `fetch` 로 할 것** |
| 로컬에서 새 빌드가 안 보임 | 옛 `next start` 프로세스가 포트를 잡고 있었다. 다른 포트로 띄워 확인 |
| 배포 직후 옛 응답 | Vercel 전파에 ~1분. 바로 검증하지 말 것 |
| `/api/config` 는 새 커밋인데 API 는 옛 동작 | **아직 그 배포가 아니다.** 커밋을 확인했다고 끝이 아니라 `?t=` 를 붙여 캐시를 피해 다시 볼 것. 실측으로 **30분 넘게 밀린 적**이 있다 — 로컬 클린 빌드가 통과하면 코드 문제가 아니니 사소한 커밋을 하나 더 얹어 배포를 다시 걸면 바로 올라간다 |
| **베르셀 배포가 조용히 멈춤** | `.gitignore` 에 `.next/` `node_modules/` 가 빠져 빌드산출물이 커밋되고 있었다(추적 304개, 195MB). 낡은 `.next`·`BUILD_ID` 가 섞여 빌드가 깨진다. **로컬 빌드는 멀쩡한데 배포만 안 되면 이걸 먼저 의심할 것** |
| 배포본이 최신인지 모르겠음 | `/api/config` 의 `commit` 필드로 확인한다. 같은 검증을 반복하지 말 것 |
| KOSIS `err 30` "활용신청 안 함" | **거짓말이다. 실제 뜻은 "해당 시점 자료 없음".** 같은 키로 2024는 되고 2025/2026만 실패하는 걸로 확인. 연 단위 지표는 최근 연도부터 되짚어 조회한다. 이 통계표는 범위조회(start≠end)도 거부한다 |
| 카카오 403 이 안 풀림 | **앱이 여러 개였다.** 에러 본문의 `App(이름)` 이 실제 판정 대상이다. 도메인·제품설정을 다른 앱에 해두면 소용없다 |
| 공공데이터포털 `SERVICE_KEY_IS_NOT_REGISTERED` | 키 문제가 아니라 **그 API 를 활용신청 안 한 것**. 포털 키는 API 마다 따로 신청해야 한다 |
| 한글이 든 `git commit -m` 실패 | 괄호·특수문자가 셸에서 깨진다. `git commit -F 파일` 로 넘길 것 |
| 특정 지역만 통계가 비거나 틀린 값 | **지역코드가 고정돼 있었다.** 주택보급률·소비심리지수는 objL1 이 경기로 박혀 있어 인천을 조회해도 경기 값이 "검증" 배지를 달고 나왔다. → `objL1BySido` (objL1=ALL 로 전 시도 실측). KB 는 강원 42→51 · 전북 45→52 로 코드가 바뀐 걸 반영 못 해 그 두 지역이 조용히 0건이었다 |
| 통합 시도에서 시도 단위 지표만 실패 | 수집 라우트가 `region.split(' ')[0]` 로 **시군구를 떼고** 넘겼다. 통합 시도는 시군구가 있어야 가린다. 수집기에 전체 지역을 넘길 것 |
| 골든 배지가 엉뚱한 지역에 붙음 | 골든 대조가 시점만 보고 **지역을 안 봤다**. 표본(경기도 광주시)과 같은 대상일 때만 대조할 것 |
| 증빙 표가 평가표와 다른 값을 강조 | 시계열 증빙이 **항상 마지막 달**을 강조했다. 지표가 채택한 달을 찾아 강조할 것 |
| 자가진단이 CD금리를 계속 실패로 표시 | 원천 이상이 아니라 **금리가 실제로 움직인 것**. 변동값은 범위 점검으로 감시한다 |
| 지도가 빈 채로 캡쳐됨 | 카카오 타일에 CORS 헤더가 없다. `html-to-image` 는 SVG foreignObject 방식이라 타일 인라인에 실패하면 **통째로 빈 그림**이 되고 사유도 안 남는다. src 를 프록시로 바꿔치기해도 같은 경로를 타서 불안정. → **DOM 을 베끼지 말고 캔버스에 직접 합성**한다(`app/captureMap.js` `composeMap`) |
| 지도가 두 번 만들어짐 | `markers` 를 `useEffect` 의존성에 그대로 넣으면 렌더마다 새 배열이라 재생성된다. JSON 비교로 고정할 것 |
| `pkill -f next-server` 가 셸을 죽임 | 패턴이 **자기 명령줄에도 들어있다**. `pkill -f "[n]ext-server"` 로 쓸 것 |
| 로컬에서 하이드레이션이 안 됨(입력이 먹통) | 옛 `next start` 가 포트를 잡고 **낡은 빌드**를 서빙 중이었다. 청크가 404/500 → React 미부착. 콘솔 404 를 먼저 볼 것 |
| 분양가 단가가 평가표와 30% 차이 | **심사는 공급면적 기준이다.** `Σ(세대수×세대당분양가) ÷ Σ(세대수×공급면적)` — 전용(`HOUSE_TY`)으로 나누면 30% 높게 나온다. 어느 기준인지 화면·엑셀에 반드시 적을 것 |
| 평당 환산이 1원 어긋남 | `3.305785` 로 반올림한 탓. **1평 = `400/121`** 로 쓰면 6,050,000원/㎡ = 평당 정확히 20,000,000 |
| 1년 이내 분양개시 판정이 틀림 | 모집공고일을 봤다. 규정상 **분양 개시일 = 공급계약시작일**(`CNTRCT_CNCLS_BGNDE`) |
| 같은 단지가 비교표에 세 줄로 앉음 | 원공고·조합원취소분·잔여세대가 각각 공고로 올라온다. 표기도 조금씩 다르다("광주탄벌 서희스타힐스2단지" / "광주 탄벌 서희스타힐스 2단지", 시도 생략). **공백 지운 단지명 + 읍면동**으로 묶고 최신 공고만 남긴다 |
| 비교사업장 수집이 느림 | 시도 공고를 전부 지오코딩하면 800건이다. **시군구 중심좌표로 1차로 거른다**(반경+25km) → 실측 832건 중 326건만 지번 조회 |
| 분양가 평균이 이상하게 낮음 | **민간임대가 섞였다.** 그 금액은 분양가가 아니라 임대보증금이다(자릿수가 다르다). 종류를 끝까지 들고 다니며 선택을 막을 것 |
| 오피스텔·도시형이 하나도 안 잡힘 | **다른 API 다**(getUrbtyOfctlLttotPblanc…). APT 분양정보만 보면 안 보인다. 서울은 이쪽이 더 많다 |
| 같은 단지가 두 줄 (조합원 취소분) | 재공고 묶음키가 괄호 **안 글자**를 안 지웠다. "○○(조합원 취소분)" 이 원공고와 따로 앉는다 |
| 입력 안 한 칸이 채워진 것처럼 보임 | **placeholder 에 그럴듯한 값을 넣은 탓.** 골든 값(`6050000`·`202607`)이나 배점(`20`)을 placeholder 로 쓰면 **입력된 값과 구분이 안 된다** — 실측으로 두 번 속았다(심사평점표 배점, 비교사업장 예정분양가). placeholder 는 `입력`·`점수`·`YYYYMM` 같은 **중립어**만 쓸 것. 값을 넣는 칸과 산출된 점수 칸도 **열을 나눠** 둘 것 |
| 빌드가 `Expected '</', got 'ident'` 로 깨짐 | **JSX 주석 `{/* */}` 을 삼항식이나 `{cond && ( )}` 안에 넣어 엘리먼트와 형제가 됐다.** 그 자리는 단일 표현식만 받는다. 설명은 일반 `/* */` 로 **JSX 밖(스타일 정의·함수 위)** 에 두거나 프래그먼트로 감쌀 것. 두 번 겪었다 — 커밋 전 `npx next build` 출력을 **끝까지** 볼 것 |
| 평균점수 항목의 총점이 소수로 어긋남 | **평가점수는 평균값이 아니라 등급 대표점수다.** 교통환경 골든이 `평균점수 2.5 · 보통` 옆에 **3점**을 적는다. 구간표 원문에는 이 관계가 없고 **실제 평가표 캡쳐에만** 드러난다 — 구간표를 넣었으면 골든 평가표로 한 번 더 대조할 것 |
| 구간표의 빈 행을 "미수령" 으로 착각 | **원문에 없는 행이었다.** 공원·문화·공공의 "3개 모두 존재" 를 반년 가까이 pending 으로 뒀는데, 가이드북 원문 표가 3행에서 끝난다. 잘린 캡쳐를 볼 때 **다음 행이 있는지**를 원문으로 확인할 것 |
| 같은 "인근 단지" 인데 결과가 다름 | **선정기준이 항목마다 다르다.** 분양가 적정성(제16조)은 준공 단지까지 쓰고 유사도를 시공순위·택지유형으로 보지만, 초기분양률은 준공을 안 쓰고 브랜드로 본다. 목록을 돌려 쓰면 안 된다 |
| **보관본을 다시 열면 수기입력이 비어 있음** | `page.js` 는 `rate/review/sheetInput` 을 넘기는데 **`storage.save()` 가 인자로 받지 않았다.** 넘기는 쪽만 보고 받는 쪽을 안 봤다. 손으로 넣은 값은 재조회로 못 되살리므로 조용한 전손이다 |
| 첫 화면에 남의 사업장이 입력돼 있음 | **골든 표본을 `useState` 초기값으로 박아두었다**(경기도 광주시 탄벌동 203-4 · 제일건설(주)). placeholder 함정과 같은 것 — 개발 편의로 넣은 값이 실무자에겐 입력된 값으로 보인다. 빈 칸으로 시작할 것 |
| 오른쪽 끝 탭에 갈 수 없음 | 가로 스크롤바를 숨겨두고(`scrollbarWidth:none`) 페이드만 얹었다. **휠은 세로로만 구른다** — 마우스만 쓰면 최종 산출물 탭에 도달할 방법이 없었다. 페이드를 버튼으로 만들고 휠을 가로로 받을 것 |
| 점검 스크립트가 엉뚱한 칸을 채움 | **모든 시트가 한 DOM 에 마운트돼 있다**(엑셀 캡쳐 때문에 `display:none` 을 못 쓴다). `placeholder` 로 칸을 찾으면 다른 탭 칸이 잡힌다 → 시트마다 `data-sheet` 이름표를 달아 두었으니 그것으로 좁힐 것 |
| [규정대로 자동선택] 이 무반응 | 요건 미달이면 **조용히 `picked: []`** 로 비웠다 — 손으로 골라둔 단지까지 날아갔다. 0건이면 지우지 말고 사유를 말할 것 |
| 콘솔에 404 가 늘 하나 | `/favicon.ico` 없음. 유일한 오류라 **진짜 오류를 이 잡음 속에서 찾게 된다**. `app/icon.svg` 로 없앴다 |
| **수집도 안 한 시트에 「1점 · 매우열악」** | `distOf` 가 null 인 이유가 둘인데 뜻이 정반대다 — ①수집했는데 부재(=base 1점이 맞다) ②아직 수집 안 함(=모른다). 둘을 같이 취급해 교통환경만 수집해도 주거편의·교육환경에 점수가 붙었고, 통계까지 수집하면 A 가 확정돼 **보증료율까지 흘러갔다**. `sheetGathered()`(=`poiDone()` 과 같은 기준)로 가른다 |
| 평균에 판정 전 기본점수가 섞임 | 교통환경·주거편의는 **항목마다 따로 판정하고 평균으로 등급을 낸다**(사용자 확인). 6차선 미판정 기본 1점이 섞인 사실을 `scoreAverage` 가 `caution` 으로 주는데 **시트·엑셀에만 적히고 A 합산표부터 버려졌다**. 평균이 흘러가는 끝까지 같이 들고 갈 것 — 실측으로 교통환경 3점이 1점이 되어 분양률이 두 급간 내려간다 |
| **비교사업장이 조용히 빠짐** (지오코딩 실패 46.7%) | 실패를 **세어보기 전엔 원인이 갈린다는 걸 몰랐다**. 정제가 못 따라간 것과 대장에 주소가 없는 것은 다른 문제다. `?census=1` 로 전수조사하고 고칠 것 |
| 신규 택지가 통째로 안 잡힘 | 지번이 아직 없다. **카카오 장소검색에 `부동산 > 부지 > 개발지구` 카테고리가 있다** — "성남낙생 공공주택지구 @경기 성남시 분당구 동원동 산 47-13" 로 지번까지 준다 |
| 지구 검색이 **엉뚱한 지구**를 줌 | 일반명사로 물었다. "공공주택지구" 만 남겨 물으니 시흥장현을 찾는데 **시흥은계**가 왔다. 고유명이 든 질의만 쓰고 **결과 이름에 그 고유명이 있는지 대조**한 뒤 채택할 것 |
| 지구명에 시군구를 붙이면 0건 | "시흥장현"·"장항지구"·"평택 모산·영신지구" 는 **지구명만** 물어야 나온다. 붙인 질의 → 지구명만, 두 형태를 다 물을 것 |
| **"지구" 도 "구" 로 끝난다** | 시군구 대조가 `장항지구` 를 시군구로 읽어 카카오가 제대로 준 "경기 고양시 일산동구 장항동" 을 **다른 지역으로 보고 버렸다**. `NOT_SGG` 로 지구·단지·신도시·국제도시·블록 꼬리를 막는다 |
| **"신도시" 도 "시" 로 끝난다** | "경기도 양주회천신도시 …" 를 시도+시군구로 읽어 질의가 "경기도 양주회천신도시 양주회천신도시" 가 됐다(0건). `headOf`·`sggOfWords` 둘 다 같은 함정 |
| 읍면동이 통째로 사라짐 | `ZONE_TAIL` 의 지구명 부분이 **문자열 앞부터 삼킨다** — "인천광역시 서구 불로동 검단신도시 AA22BL" 이 통째로 지워졌다. 정규식으로 자르지 말고 **앞에서부터 행정구역 토막만 읽을 것**(`adminPrefix`) |
| **행정구역 개편으로 멀쩡한 매칭을 버림** | 공고 "인천광역시 **서구** 불로동" ↔ 카카오 "인천 **검단구** 불로동". 인천 실패 17건 중 15건이 이것이었다. 매핑을 손으로 적지 말 것 — **읍면동을 카카오에 물으면 옛 표기로 물어도 현재 시군구를 돌려준다**(불로동→검단구 · 운서동→영종구 · 가정동→서해구 실측) |
| **본건(심사대상)이 비교사업장 목록에 앉음** | 실측: 용답동 108-1 로 조회하니 「청계리버뷰자이 · 0m」 가 1번 줄. 자기 자신이 평균에 들어가면 분양가격지수가 1.00 쪽으로 끌린다. 지우지 말고 **「본건」이라 적고 선택만 막을 것**(`siteAddress` 를 넘겨 지번 대조) |
| 반경 1km 인데 단지가 3~4곳뿐 | 버그가 아니라 **원천 범위**다. 청약홈은 2020-02 부터라 기축 단지가 없다(용답동 1km: 청약홈 4건 vs 카카오 아파트 16곳). 카카오+K-apt+실거래가로 **별도 표**에 올리되 분양가가 없다고 적을 것 |
| **반경이 시군구를 넘는다** | 사업지는 성동구인데 반경 1km 안 단지 14곳 중 8곳이 동대문구였다. 사업지 시군구 색인 하나만 받으면 그 8곳은 실거래도 세대수도 못 채운다 — **단지 주소의 시군구별로** 색인을 받을 것 |
| 도로 후보가 50줄 넘게 쏟아짐 | 실측(용답동 1km) 66곳 중 **대로 2 · 로 9 · 길·번길 55**. 기본은 대로·로만 펴고 길은 접을 것 |
| 도로 핀이 배지 거리와 어긋나 보임 | `coord2address` 는 **표본점이 속한 필지**의 도로명을 준다 — 도로 중심선이 아니다. 표본점을 버리지 말고 다 들고 와 **고른 도로가 지나는 자리를 전부 찍을 것**(번호·라벨 없는 자취 점) |
| 시설을 지웠는데 점수가 그대로 | 판정은 `nearest.distance` 로 난다. 화면에서만 빼면 안 되고 **목록·최근접·건수를 한자리에서 다시 계산**해야 한다(`applyHidden`) — 시트·지도·엑셀이 같은 파생본 하나만 보게 할 것 |
| 근거 칸이 검산이 안 됨 | "지하철역 5 + 6차선 왕복도로 4 ÷ 2" 처럼 괄호가 없고 실측치가 없었다. **실측치(거리·차선·순위)를 앞에 적고 `(a + b) / 2 = 평균 → 등급 → 대표점수`** 로 끝맺을 것 |
| 증빙 지도에서 시설명을 못 읽음 | 420px 에 라벨 8개 상한이었다. **지도 640px · 캡쳐 3배 해상도 · 엑셀 이미지 1240px · 라벨 상한 해제.** 타일은 1배라 한계가 있지만 핀·라벨·반경원은 캔버스에 직접 그리므로 배율만큼 선명해진다 |
| 탐색 창구 raw 가 401 SERVICE_KEY_IS_NULL | 키 문제가 아니라 **`path` 에 질의문자열이 붙어 물음표가 두 번** 찍힌 것이었다 |
| HUG 오픈API 가 `code -3 등록되지 않은 서비스` | 포털 화면이 JS 렌더라 엔드포인트를 안 보여주고, uddi 로 만든 odcloud 경로는 안 열린다. **같은 통계가 KOSIS 에 있는지 먼저 볼 것** — HUG 는 orgId=414 로 9개 표가 올라와 있다 |
| KOSIS 가 itmId·objL1 을 줬는데도 "자료 없음" | **2단 분류표는 `objL2` 도 필요하다**(HUG 분양가격 = 지역 × 규모) |
| KOSIS 단일 시점이 err 30 인데 범위는 됨 | **표마다 반대다.** 주택보급률은 범위조회를 거부하고 단일 시점만 받는데, HUG 초기분양률은 단일 시점을 거부하고 범위만 받는다. 둘 다 err 30("활용신청 안 함")으로 위장한다 |
| 기준선이 정작 필요한 때 안 보임 | 지역 평균 줄을 `items.length > 0` 안에 넣어 **반경 안에 단지가 0건일 때 사라졌다** — 그때가 가장 필요한 때다. 빈 결과에도 보이는 자리에 둘 것 |
| **안내문이 가리키는 버튼이 화면에 없음** | 경계 그리기에서 "「이 경계로 수집」 을 누르세요" 라고 적어놓고 그 이름의 버튼이 어디에도 없었다 — 실제로는 **674px 위** 단계 줄의 [반경시설 수집] 을 다시 눌러야 했다. 안내문을 쓸 때는 **그 이름의 버튼이 실제로 있는지** 확인할 것. 수집 버튼을 두 군데 두지 않는 규칙은 지키되, **흐름을 끝내는 버튼은 그 흐름이 벌어지는 자리에** 둔다(경계 기준을 고른 그 순간에만 나타났다 사라진다) |
| **3배로 찍기로 해놓고 2배로 찍힘** | `composeMap` 기본값을 3 으로 올렸는데 부르는 쪽 `captureMap({ pixelRatio = 2 })` 가 덮고 있었다. 실측 엑셀 지도 원본 2096x1280 → 배치 1240px 대비 **1.69배 여유뿐**. 기본값을 두 곳에서 따로 정하지 말 것 |
| 엑셀이 47MB | 지도를 3배 **PNG** 로 넣은 탓. **타일은 연속톤 사진이라 PNG 가 최악이다** — JPEG 0.92 로 바꾸니 **47MB → 8MB**, 확대여유 2.68배는 그대로. 핀·라벨 글자는 크고 진해서 JPEG 에서도 읽힌다. 표·글자 증빙(카드)은 PNG 를 유지 |
| **[이름표 끄기] 가 화면에서 안 먹음** | 상태를 **그 상태를 실제로 쓰는 effect** 가 아니라 옆 effect 의 의존성에 넣었다. `labels` 가 **캡쳐 등록 effect 에만** 들어 있어 끄면 엑셀·PNG 캡쳐는 꺼지는데 **화면 이름표는 그대로** 남았다(실측 22개 → 끈 뒤에도 22개). 버튼 색만 바뀌니 "눌렀는데 안 먹는다" 로 보인다. 「화면에서 끄면 캡쳐도 같이 꺼지게」 가 **거꾸로** 나 있던 셈이다. 지도를 다시 그려 고치지 말 것 — 타일을 다시 받고 확대·이동이 초기화된다. **오버레이를 ref 에 들고 `setMap(map|null)` 로 켜고 끈다.** 그릴 때는 ref 로 현재 상태를 읽어 **끈 채로 다시 그려져도** 안 켜지게 한다. (지도타입 버튼은 onClick 에서 `setMapTypeId` 를 직접 불러 멀쩡했다 — 죽은 버튼은 이것 하나였다) |
| 지도가 흰 박스로 덮임 | 라벨 상한을 없앤 뒤 시설이 많은 지역에서 이름표가 지도를 가린다. **[이름표 끄기]** 로 번호 동그라미만 볼 수 있게 두되, **화면에서 끄면 캡쳐도 같이 꺼지게** 할 것 — 증빙이 화면과 달라지면 안 된다 |
| **지도가 짧아 시설 확인이 안 됨** | 이 지도의 목적은 증빙 그림이 아니라 **시설이 진짜 거기 있는지 눈으로 확인**하는 것이다(사용자 지적). 높이 640→**760px**, 지도 바에 **[크게 보기]**(창 전체·Esc로 닫기). 다만 1440x900 에서 전체화면은 817px 라 **높이는 7% 밖에 안 는다** — 크기보다 **축척**이 본질이었다 |
| **반경원이 화면의 46% 밖에 안 씀** | `setBounds` 는 "들어가기만 하면 되는" 단계를 고르므로 **한 칸 덜 확대**되곤 한다. 투영(`containerPointFromCoords`)으로 **원의 지름을 픽셀로 재서** 짧은 변의 70% 를 넘을 때까지 당긴다 → 실측 46% → **93%**. 건물 상호가 읽히는 수준이 된다. 단 **반경 밖 마커(6차선 도로 후보는 반경 1.2배)가 있으면 당기지 않는다** — 그 핀이 잘린다 |
| 크게 펴면 오히려 더 작아 보임 | `flex` 로 커지는 높이가 **지도 생성 시점엔 아직 확정되지 않아** 옛 축척이 남는다. `relayout()` 뒤 한 번 더 맞출 것 |
| 시설 마커만 담아 판정선이 화면 밖으로 | bounds 에 **반경원도 함께 담는다** — 시설만 담으면 "이 원 안에 있는 것들" 이라는 그림이 깨진다 |
| 의료시설이 카카오로 걸러지는 것처럼 보임 | **판정은 심평원이 맞다**(route 가 카카오 결과를 받은 뒤 의료시설만 `collectMedical()` 로 덮어쓴다). 그런데 **화면도 엑셀도 원천을 말하지 않았고**, 엑셀은 반경시설 증빙에 `출처 : 카카오맵` 을 의료시설에도 찍었다 — 틀린 출처를 단 증빙이 심사 파일로 들어갔다. 봉투에 `source` 를 실어 값과 같이 들고 다닐 것 |
| 심사평점표 값 칸에 「점수」라고 적힘 | 그 칸은 **원시값**(1.05 · 4.51% · 17위)을 받는다. `reviewScore` 가 `band` 를 판정 라벨 문자열로 **덮어쓰며 구간표의 `unit` 을 지워** 화면이 기본값 「점수」로 떨어졌다. 덮어쓸 값이 있으면 원본에서 필요한 것을 먼저 빼둘 것 |
| 끝난 단계가 화면을 계속 먹음 | 심사평점표 탭인데 900px 중 **830px 이 입력폼·지도·보관목록·통계카드**였다. 입력칸과 결과가 한 화면에 없으면 눈도 마우스도 왕복한다 — **그 단계의 도구는 그 단계에서만 편다** |
| 오른쪽 끝에 붙였더니 더 멀어짐 | `marginLeft:'auto'` 로 결과를 밀었더니 입력칸(x=182)과 **1000px** 떨어졌다. 세로 이동을 없앴을 뿐 가로로 더 멀어진다 — **바로 옆**에 붙일 것 |
| **구간표를 받았는데 시트에는 점수가 안 찍힘** | 구간표를 받으면 고칠 곳이 **두 군데**다 — A 합산표(`src/lib/manual.js`)와 **시트 정의(`app/sheets.js`)**. 지역미분양·지역수요를 manual.js 만 고쳐두어, 정작 실무자가 눈으로 옮겨 적는 시트·엑셀 증빙의 평가기준·평가점수 칸이 빈 채였다(2026-09-17 실사례 심사에서 발견). 시트가 [수기입력] 탭 값을 쓰면 `sheetInput` 을 `buildSheet` 까지 넘길 것 |
| **증빙에 API 키가 박혀 나감** | KOSIS 조회 URL 의 `apiKey` 가 증빙 카드·엑셀의 「조회 URL」 로 그대로 나갔다. 심사 엑셀이 밖으로 전달되면 키가 같이 나간다 — 봉투로 나가는 url 은 키를 지운다(`kosis.maskKey`). 재현에 필요한 것은 조회조건이지 키가 아니다 |
| 로드뷰가 회색 박스로만 뜸 | 이 샌드박스 프록시가 `map_roadview` 타일을 막을 때가 있다(ERR_FAILED). 앱 버그가 아니다 — 차선은 **위성 확대**로 세고, 실사용자는 로드뷰로 센다 |
| **백화점·공원이 통째로 안 잡힘** | 카카오는 한 질의에 **45건(15×3)** 까지만 준다. 일반명사 키워드를 **거리순**으로 물으면 그 45칸을 **그 건물에 입점한 매장들**이 0m 로 채운다 — "백화점" 질의에 「세라 롯데백화점구리점 · 티쏘 롯데백화점 구리점 · 카페연무장 …」 이 앞을 다 먹고 정작 「롯데백화점 구리점」(15m)은 밖으로 밀렸다(구리 실측: 반경 1.8km 239건 중 거리순 45건에 백화점 분류 **0건**). **정확도순(sort=accuracy)은 1건으로 준다** → 두 정렬을 모두 받아 합친다. 분류 필터는 그 뒤 이야기다 — 목록에 들어오지도 못하면 걸러볼 것조차 없다 |
| **도로 배지 거리가 지도와 안 맞음** | 원인이 둘이다. ① 1km 를 **균일 120m 격자**로 훑고 거리를 **격자점까지**로 쟀다 — 도로가 붙어 있어도 120m 가 하한이다(수원 수인로 실측 120m → 실제 경계에서 7m). 구간표가 100/300/500/1000m 에서 갈리므로 **가까울수록 촘촘하게** 훑을 것(150m 안 25m · 350m 안 50m · 700m 안 100m · 그 밖 150m). ② 다른 시설은 경계 최단거리인데 **도로만 대표지번 중심**이었다. 배지에 **±격자간격과 기준(경계/중심)** 을 같이 적어야 무엇을 잰 값인지 읽힌다 |
| **미분양비율의 분자·분모 시점이 어긋남** | 주민등록세대수(KOSIS)는 미분양(통계누리)보다 **한 달쯤 앞서** 공표된다(실측 2026-09-23: 세대수 202608 · 미분양 202607). 같은 조회월로 요청하니 정상경로에서는 같지만, 원천이 그 달을 안 주기 시작하면 조용히 어긋난다. **시점이 다르면 비율을 내지 말고 두 시점을 적을 것** — 시트에 「기준시점」 열을 둔다 |
| **화면에는 있는데 엑셀에는 없음** | 카카오 제외 건수·45건 상한을 화면에만 적고 엑셀 출처 줄에는 안 실었다(2026-09-23 실측 — 화면 「분류가 맞지 않아 92곳 제외」, 엑셀은 거기서 끝). 의료시설은 실려 있어 더 헷갈린다. **증빙이 화면과 달라지면 안 된다** — 화면에 적는 문구를 추가하면 `exportExcel.js` 의 출처 줄도 같이 고칠 것 |
| **제외 라벨이 검색어를 되풀이해 아무것도 설명 못 함** | 부천 상동 실측 — 공공시설이 「분류가 맞지 않아 12곳 제외 (**도서관 10**)」 로 찍혔다. 「도서관을 왜 빼느냐」 로 읽힌다(국립어린이청소년도서관 때와 같은 오해). 실제는 작은도서관 8 · 사설 영어도서관 2 다. 원인 둘 — ① 카카오 분류 `교육,학문 > 학습시설 > 도서관 > 작은도서관` 에서 **3번째 토막만** 보아 둘이 뭉쳤다 ② 「분류가 맞지 않아」 는 **어떤 기준에 안 맞았는지**를 말하지 않는다(의료시설은 「병원급이 아닌」 이라 읽힌다). **말단으로 통째로 바꾸지 말 것** — 같은 실측에서 상업시설이 나빠진다(「대형슈퍼 9」 → 「홈플러스익스프레스 6 · 노브랜드 2 …」, 사유가 사라진다). **검색어와 같은 말이 라벨로 나올 때만** 한 단계 더 들어가고, 시설마다 채택 기준(`source.accept`)을 봉투에 실어 「공공기관·국공립도서관이 아닌 12곳 제외」 로 적는다. 조사(이/가)는 수집기 한 곳에서 붙인다 |
| **주버튼이 한 화면에 넷** | 파란색을 **세 가지 뜻**으로 썼다 — 고른 상태(`1km`·`공급면적`·`세대수 가중`)와 실행(`반경 1km 분양단지 수집`)이 같은 진한 파랑이라 "지금 뭘 눌러야 하나" 가 갈렸다. **고른 상태는 옅게(accentSoft+밑줄), 실행만 진하게.** 실측 주버튼 4 → 1 |
| **끝났는데 끝난 것처럼 안 보임** | ① 탭 옆 완료 점이 **앞 8개 탭에만** 있었다 — 정작 값을 넣는 비교사업장·수기입력·초기예상분양률·심사평점표엔 없었고 그 넷은 탭 줄 오른쪽 끝이라 스크롤 밖일 때가 많다 ② 수집 버튼이 끝나도 진한 파랑 그대로라 또 눌러야 하는 줄 알았다(다른 버튼은 ✓ 초록으로 바뀐다) ③ **이미 그 탭에 있는데** 단계 줄 `[비교사업장 →]` 가 계속 파란 다음 버튼이었다 |
| **막다른 길 — 0건일 때 다음이 없음** | 반경 안에 비교단지가 0곳이면 표에 본건만 남는데 화면은 「아래 표에서 고르세요」 라고만 했다(실측: 부천·여주·평택 **세 곳 다** 규정 기본반경에서 0곳). 규정 제16조 **비고1**(0건이면 매 1km 확대) 문구와 `[반경 넓히기]` 를 **그 자리에** 둘 것 — 반경 줄은 한참 위다 |
| **조건이 걸렸는데 말하지 않음** | 「100세대 미만이면 60% 상한」 이 1,859세대에도 99세대에도 **같은 문장**이었다. 상시 안내문과 실제 발동을 구분할 것 — 해당하면 「60% 상한 적용 대상」, 값이 내려갔으면 `(70% → 60%)`, 급간이 이미 60% 이하면 그렇게 적는다 |
| **같은 글자 버튼이 한 시트에 두 쌍** | 교통환경 시트에 지도 확대·축소 `[＋][－]` 와 차선 증감 `[＋][−]` 가 같은 글자였다 — 점검 스크립트가 **지도만 여섯 번 확대하고 차선은 0** 인 채 넘어갔다(사람도 같은 실수를 한다). 게다가 6차선을 넣으려면 여섯 번 눌러야 했다. → 차선은 `[2][4][6][8]` 칩으로 한 번에, 스테퍼는 `▲▼` 로 글자를 가른다 |
| **넣는 버튼이 넣는 칸에서 280px** | 심사평점표 `[7위 넣기]` 가 **근거 칸**에 있었다. 비교사업장은 입력칸 바로 옆이라 **문법도 갈렸다**. 값을 넣는 버튼은 값 칸에 둔다 |
| **일반구가 있는 시에서 기축단지 보강이 통째로 죽음** | K-apt·실거래 색인은 **구 단위**인데(부천 원미 41192·소사 41194·오정 41196) 단지 주소 앞 **두 토막**만 떼어 「경기 부천시」(41190)로 물어 **0건** → 세대수·시공사·사용승인일·실거래가 전부 빈 표. 그런데 화면은 「**이름이 맞지 않은 것**」 이라고 **틀린 이유**를 댔다. 수원·성남·안양·안산·고양·용인·창원 등이 같은 증상이다. 구가 있으면 세 토막을 쓸 것(「지구」도 「구」로 끝나므로 막는다). **색인을 못 받은 것과 이름이 안 맞은 것은 다르다** — `kaptIndexed` 로 가른다 |
| **게이트웨이 오류가 개발자 메시지로 노출** | `Unexpected token 'u', "upstream r"... is not valid JSON` 이 그대로 화면에 떴다. `await res.json()` 을 곳곳에서 직접 부르면 본문이 JSON 이 아닐 때 파서 오류가 사용자에게 간다 — **`app/fetchJson.js` 한 곳**으로 모아 사람이 읽을 말로 바꾼다 |
| **접힌 손잡이가 지금 상태를 말하지 않음** | [반경시설 수집] 을 눌러 **이미 그리기 모드**인데도 손잡이가 「경계 미지정 — **[반경시설 수집] 을 누르면** 여기서 그립니다」 그대로였다. 이미 누른 사람에게 "버튼을 또 누르라" 로 읽힌다 — 할 일은 지도를 찍는 것이다. 상태별로 갈라 적을 것(수집 전 / 그리기 중 N점 / 3점 이상) |
| **판정 안 한 항목이 섞인 채 보증료율까지 감** | 「비어 있으면 심사평점표로 못 넘어가게」 라는 지적에서 나왔는데, **비어 있는 것은 이미 막힌다**(A 가 null 이면 합계도 등급도 안 난다). 새는 것은 **비어 있지 않은데 판정을 안 한 쪽**이다 — **6차선 차선 수·인구유입요인은 안 넣으면 비는 게 아니라 기본 1점이 들어간다.** 그래서 A 가 「나와버리고」 종합평가 → 초기예상분양률 → 초기분양률(22) → 종합평점 → 심사등급 → 보증료율까지 아무 말 없이 간다. `provisional` 은 수기입력 탭에서만 적혀 있었다(실측: 차선 미입력 종합 75 / 입력 후 77 — 75 는 급간 경계 바로 위). → 섞였으면 **초기예상분양률을 심사평점표로 넘기지 않는다**(page.js 한 지점에서 끊는다). **탭은 막지 말 것** — 사업수익률·자기자금·신용등급은 이 앱이 수집하지 않는 값이라 사업수지표를 든 실무자가 먼저 넣을 수 있어야 한다. 막는 것은 결론이다 |
| **막힌 이유를 찾아 세 번 튕김** | 심사평점표에서 「초기예상분양률 탭으로 →」 → 거기서 「비교사업장 탭으로 →」 → 거기서 「수기입력 탭에서 완성하세요」. **관문을 한 장으로 모은다** — 통계·시설·판정 전 항목·수기입력 A·비교사업장·본건 분양가를 ✗/✓ 로 세우고 각 줄에서 바로 간다. 사유 줄은 **이름만 적으면 안 된다** — 「지역수요」 라고만 하면 통계를 이미 받았는데 뭘 더 넣으란 말인지 모른다(실제로는 인구유입요인 개수다). 행이 들고 있는 `why` 를 그대로 쓸 것 |
| 엑셀에 탭이 통째로 없음 | 빈 탭 확인창이 "비어 있는 칸은 빗금으로 들어갑니다" 라고 해놓고 **비교사업장은 시트 자체를 안 만들었다**(`compare?.data?.items?.length` 가드). 받는 쪽은 탭이 사라진 이유를 알 방법이 없다 — 수집 안 했으면 **그렇게 적힌 빈 시트**를 만들 것 |
| **통계가 한 달씩 뒤처짐** | 전 지표를 조회월 하나(`/api/latest` = 미분양 최신월)에 묶고 있었다. 미분양이 가장 늦게 나오므로 **다른 원천이 더 최신을 갖고 있어도 거기 맞춰 뒤처진다** — KB 매매지수는 202608 이 있는데 202607 을 쓰고 있었다(골든도 202608 이다). `config/indicators.json` 의 `periodPolicy` 로 가른다: `anchor`(미분양·주민등록세대수 — 둘은 같은 달이어야 비율이 성립) / `latest`(KB·소비심리·주택보급률 — 시점을 안 넘기면 수집기가 최신을 찾는다). 골든 자가진단은 시점을 명시해 부르므로 영향 없다. 시트에 「기준시점」 열을 둬 어느 달 값인지 보이게 할 것 |
| 샌드박스 브라우저로 실사이트 확인 불가 | **틀린 기록이었다(2026-09-15 정정).** Chromium 이 `HTTPS_PROXY` 를 안 쓰고 있었을 뿐이다. `chromium.launch({ proxy: { server: process.env.HTTPS_PROXY }, args: ['--ignore-certificate-errors'] })` 로 띄우면 **배포본이 그대로 열리고 카카오 지도도 뜬다**(도메인 제한 통과). 다만 프록시가 간헐적으로 502 `upstream request failed` 를 낸다 — 내 앱 오류로 오해하지 말고 재시도할 것. 로컬(localhost)은 여전히 카카오 JS 키 도메인 제한으로 지도가 안 뜬다 |

## 구조

```
config/indicators.json   지표 카탈로그 (원천·파라미터·골든값·원문링크) — 단일 진실공급원
src/collectors/          molit · kb · kofia · kosis · constructor · kakao · hira(의료시설) · applyhome(분양가)
src/lib/                 http(재시도·표준봉투) · env · region(시군구코드) · geo(폴리곤거리)
                         scoring(구간표 판정·분양률 급간) · compare(분양가경쟁력 산식 단일지점)
app/api/                 collect · facilities · apts(비교사업장) · kosis/applyhome/hira(탐색) · health(키진단) · config(JS키·배포커밋)
                         selftest(골든 재현 감시, 매일 09시 cron, 실패시 503)
app/                     page · SheetTabs · SheetView · CompareView(비교사업장) · RateView(초기예상분양률)
                         ReviewView(심사평점표) · EvidenceCard · RadiusMap · Overview
                         PolygonDrawer(사업지 경계) · kakaoSdk · storage/SavedList(브라우저 보관)
                         exportExcel(ExcelJS, 시트별 표+캡쳐이미지) · SourceHealth(원천 상태배지)
tools/                   헤드리스 캡쳐/지도 — 로컬 배치 전용. 웹앱 번들에 넣지 말 것
docs/evidence-samples/   골든 캡쳐 11장
```

수집 결과는 **표준 봉투**로 통일한다 — 값만이 아니라 `citation`(엑셀에 넣을 출처문구) ·
`queryParams`(재현용 조회조건) · `dataUpdatedAt`(원천 갱신일) · `viewUrl`(원문화면) 을 항상 동반한다.

## 완료 (되돌리지 말 것)

- **사업지 폴리곤** — 지도에서 직접 그리고 **경계 최단거리**로 판정(사용자 확정).
  실측 차이: 초등학교 457m→343m, 공원 183m→71m, 공공시설 280m→149m. 100m 넘게 벌어진다.
  경계 미지정이면 대표지번 중심점 기준. 결과의 `basis`(polygon/point)로 구분된다.
- **POI 시트 3종은 표 구조가 다르다**(캡쳐대로). 하나로 합치지 말 것.
  교통환경 `flat` · 주거편의 `grouped`(2개 그룹+묶음라벨) · 교육환경 `dual`(500m/1km 2단)
- 브라우저 보관(localStorage, 서버 저장 없음) · 엑셀 내보내기 · 원천 감시 배지
  **엑셀은 화면 상태를 그대로 읽는다** — `[이 조회 보관]` 을 누르지 않아도 수기 판정값까지 다 들어간다.
  보관은 나중에 다시 열기 위한 것이지 내보내기의 전제가 아니다.
- **의료시설 심평원 연동 완료**(2026-09-14 활용승인). 광주시 검증: 22곳 수신 → 전부 의원급 → 부재.
  제외된 의원급 수를 `excludedClinics` 로 남겨 "왜 부재인가"를 증빙에 설명한다.
- **지도 캡쳐는 캔버스 직접 합성**(2026-09-14). 타일만 `/api/tile` 로 같은 출처로 받아 찍고,
  반경원·사업지 경계·마커·라벨은 `Projection.containerPointFromCoords` 로 좌표에서 다시 그린다.
  반경 픽셀은 정북 radius m 지점을 투영해 재므로 축척 가정이 없다. 검증기록: `docs/QA-지도캡쳐.md`
- **타일은 1배, 우리가 그린 것은 3배다**(개념 · 실측 2026-09-17).
  지도는 두 층이다 — **아래 = 카카오가 보내주는 타일 사진**, **위 = 반경원·경계·핀·이름표**.
  `composeMap` 은 위층만 좌표에서 다시 그리므로 배율만큼 선명해지고,
  아래층은 이미 256px 사진이라 3배 캔버스에 **늘려 붙일 뿐**이라 뿌옇다.
  판정 근거로 읽는 것은 위층이므로 실용상 문제는 없다.
  **고해상도 타일은 존재한다** — DPR 2 로 띄우면 `PNGSD02`(256px) 대신 `PNG02`(512px) 를 준다(실측).
  다만 **HD 는 버전 해시가 따로다**: `PNG02/v23_bkf12`(SD 해시) 도 `PNG02/latest/latest` 도
  512x512 헤더에 **내용이 빈 0KB** 를 주고, `PNG02/v23_oda0e`(HD 해시) 만 26KB 실제 그림이 온다.
  그 해시는 카카오가 관리하고 **DPR 1 화면에서는 DOM 에 나타나지도 않는다** — 하드코딩하면
  해시가 바뀌는 날 지도가 통째로 빈 회색으로 찍힌다(이미 겪은 함정이다). **그래서 손대지 않았다.**
  굳이 한다면 **HD 를 먼저 받아보고 빈 이미지면 SD 로 되돌리는 폴백**이 전제다.
- **반경은 사업지 경계 버퍼로 그린다**(2026-09-14). 대표지번 중심의 원은 판정선과 다르다 —
  1km 기준에서 중심원이 모든 방향으로 100~181m 작았다. `geo.bufferPolygon()` 이
  판정에 쓰는 `distanceToPolygon` 을 이분탐색으로 역산하므로 **그린 선이 곧 판정선**이다
  (실측 편차 0.00m). 지도 바의 [경계기준]/[중심기준] 으로 바꾼다.
- **행정구역은 목록에서 고른다** — `data/regions.json` (전국 228 시군구 + 세종).
  생성: `tools/build-regions.mjs` ← 통계누리 미분양 formId=2082.
  별도 키가 필요 없고 **미분양 수집기와 표기가 같아** 표기차로 0건이 나지 않는다.
  시군구만 쳐도 시도가 따라오고, 동명 지역(고성군·광주)은 시도를 붙여 **전부** 보여준다.
- **상세주소는 행정구역에 이어붙여 카카오 주소검색에 통째로 넘긴다.**
  지번·도로명 둘 다 된다. 0건이면 장소검색(단지명·상호)으로 폴백한다.
  **장소검색은 시군구를 넘어간다** — 실측 "경기도 광주시 롯데마트" 7건 중 6건이 성남시 분당구.
  그래서 `region` 을 함께 넘겨 시군구 밖 후보를 걸러내고 제외 건수를 화면에 표시한다.
  전부 시군구 밖이면 지우지 않고 경고한다(`outOfRegion`).
  주소 불일치는 500 이 아니라 **404** 로 구분해 돌려준다 — 사용자가 고칠 수 있는 입력 문제다.
- **주소 매칭을 사용자가 확인한다.** 첫 결과 자동채택은 금지 — 후보를 보여주고 고른 좌표를
  `x,y` 로 넘겨 서버가 다시 고르지 않게 한다. "외 57필지" 꼬리표는 조회 전에 떼어낸다.
- **흐름**: 주소 확정(잠금·초기화) → 사업지 경계 → 통계 수집 → 시트별 반경시설 수집.
- **차선 판정은 로드뷰**로 한다(사용자 확정: "로드뷰. 맞아 이게 정답이다").
  위성은 가로수·그림자·차량에 가려 못 센다. 모든 반경 지도에서 열 수 있고,
  6차선 판정 지도는 처음부터 펴 둔다. 지도를 클릭하면 그 지점 로드뷰로 이동한다
  (`RoadviewClient.getNearestPanoId`, 120m). 로드뷰는 캔버스 렌더라 **캡쳐 불가** —
  증빙은 위성 지도로 남긴다.
- **수집 기준의 기본값은 경계다**(사용자 확정 2026-09-17). 규정이 재는 거리가
  「단지 경계로부터 시설물까지」라 경계 기준이 규정대로다 — 중심점은 100m 넘게 멀게 잡힌다(실측).
  [반경시설 수집] 은 묻지 않고 **바로 경계 그리기로 간다.** 중심 기준은 단계 줄의 [변경] 으로만 고른다
  (그때 옛 관문 [사업지 경계 기준]/[대표지번 중심 기준] 이 열린다).
  **기본값을 경계로 바꾸면 지도 자동 펼침 조건을 같이 손봐야 한다** — `basisMode === 'polygon'` 은
  이제 주소 확정 직후부터 참이라 그것만 보면 지도가 350px 을 먹던 옛 함정으로 되돌아간다.
  `pending !== undefined`(수집을 시작했는가)를 같이 본다.
  수집 버튼은 단계 줄에 **하나만** 둔다(지도 안에 또 두지 말 것).
- **반경 내 시설은 지도에 전부 찍는다.** 핀 번호 = 표의 #. 라벨은 겹치니 자리잡기로 놓고,
  자리가 없으면 라벨만 포기한다(핀은 남는다).
- **시트는 시설마다 [표 → 지도] 한 덩어리**로 놓는다. 표를 모아놓고 지도를 모아놓으면 못 맞춘다.
- **조회월은 자동**(`/api/latest` 가 통계누리를 되짚어 최신월 판정). 평가연도 입력폼은 없앴다 —
  적재된 공시 중 최신을 쓴다.
- **시공사는 주소와 무관**하다. 주소 확정 후에도 바꿀 수 있어야 한다.
- **자가진단은 두 가지 모드**를 쓴다. 고정 정답(`golden.value`)과 범위(`golden.sanityRange`).
  매일 변하는 값을 고정 정답으로 감시하면 경고가 상시화되어 진짜 이상을 놓친다.

- **비교사업장 탭**(2026-09-14). 반경 1/2/3km 안에서 분양한 아파트를 청약홈에서 찾아
  **전용면적 기준 ㎡당 분양가**를 내고, 고른 단지들의 **산술평균**을 실시간으로 보여준다.
  단지 대표단가는 [세대수 가중]/[단순평균] 을 고르게 하고 **어느 쪽을 썼는지 화면·엑셀에 같이 적는다**
  (실무 관행이 갈린다). 고른 단지는 면적별 세대수·분양가를 별도 그리드로 펼친다.
  거리·반경은 다른 시트와 같은 규칙 — 경계 기준으로 그릴 때만 경계 최단거리로 잰다.
  **종류(아파트·민간임대·오피스텔·도시형생활주택)를 칩으로 켜고 끈다**(사용자 요청).
  기본은 아파트만. 종류를 끄면 그 종류로 고른 것도 평균에서 뺀다.
  분양전환 임대는 분양가가 없어 표에서 빼되 **반경 안에 몇 건 있었는지 화면·엑셀에 남긴다** —
  조회 단계에서 조용히 버리면 "옆에 단지가 있는데 왜 안 나오나" 에 답을 못 한다.
  검증기록: `docs/QA-비교사업장.md`
  **반경 안을 먼저 다 담고 단계별로 거른다**(사용자 확정 2026-09-17).
  좌표를 못 찾았다고 조용히 버리지 않고 택지지구까지 찾아본 뒤 반경 판정에 넣는다.
  **비교단지는 우리 정보가 가진 사실을 적시하면 된다**(사용자 확정 2026-09-17) —
  거리·분양개시일·시기·단가·유사도·깔때기 건수가 그 사실이다. 몇 곳으로 평균을 낼지는 실무자가 판단하므로
  "반경을 넓혀 보라" 같은 권유는 넣지 않는다(규정 비고1의 매 1km 확장은 **0건일 때** 문구라 그대로 둔다).
  그래도 못 찾은 것은 **목록이 아니라 건수 한 줄**로 남긴다 — 같은 시군구를 전부 적으면
  반경과 무관한 단지까지 길게 늘어놓게 된다. 어느 단계에서 몇 건이 빠졌는지는 표 위 깔때기 줄이 말한다.
- **분양가 적정성 규정 반영**(2026-09-15). 심사지침 제16조 · 인근 유사사업장 요건(3개) ·
  사례해설 EX1~4 · 실무 Q&A Q6~Q11 · 실제 평가표를 한 줄씩 대조해 넣었다.
  **대조표는 `docs/규정-분양가적정성.md`** — "왜 이렇게 판정하나" 의 근거가 전부 거기 있다.
  · 면적기준 **공급면적(심사기준)** 이 기본, 전용면적도 고를 수 있다
  · 반경 1~5km, **규정 기본값을 지역으로 자동 결정**(수도권·광역시 1km, 그 밖 2km)
  · 시기 3분류(1년 이내 분양개시 / 분양 진행중 / 준공) — **분양개시일 = 공급계약시작일**
  · 유사도 4항목 자동 판정. 시공순위는 적재해 둔 공시 명부로 조회, **택지유형은 원천에 없어 미상**
  · **[규정대로 자동선택]** — 2개 이상 → 공공분양·10년경과 제외 → 1년이내 우선 → 3개이상 우선
  · 분양가격지수 · 배점표(`분양가경쟁력` matrix) · 제16조 적정분양가 판정 문구
  **자동으로 지우지 않는다** — 제외 권고는 배지로만 달고 판단은 실무자가 한다(본·지사 협의 사항).

- **가이드북 원문 구간표 반영**(2026-09-16). 「금융심사처 실무 가이드북(PF보증 심사지침)_250613」 캡쳐 수령.
  · **지하철역** — 오래 pending 이던 "존재" 구간을 받았다. 100m/300m/500m/1km = 5/4/3/2점, 1km 부재 = 1점.
    **6차선 왕복도로와 같은 표다.** 거리는 "아파트 단지 경계로부터 시설물까지"(Q1) — 이 앱의 경계 기준이 규정대로였다.
  · **공원·문화·공공의 "3개 모두 존재" 행은 처음부터 없다.** 원문 표는 3행뿐(2개 존재 3점 / 1개 존재 2점 / 부재 1점).
    캡쳐가 잘린 게 아니라 그렇게 끝나는 표였다 → pending 해제. 2개든 3개든 3점.
  · **교통환경·주거편의는 항목 점수의 평균값으로 등급을 낸다** (`등급:5점척도`, `scoreAverage()`).
    4점초과~5 매우양호 / 3초과~4 양호 / 2초과~3 보통 / 1초과~2 열악 / 1 매우열악.
    **평가표에 적히는 평가점수는 평균값이 아니라 그 등급의 대표점수다**(5/4/3/2/1).
    골든 검산: 교통환경 (1+4)/2 = 2.5 → 보통 → **3점** · 주거편의 (1+2)/2 = 1.5 → 열악 → **2점**.
    상업·의료에 **7점** 행이 있는 이유가 이것이다 — 평균을 내면 5점 척도 안에 들어온다.
  · **브랜드경쟁력** — 1~10/11~20/21~50/51~100/101위이하 = 5/4/3/2/1점.
    골든 검산: 제일건설(주) 17위 → 4점 양호 (캡쳐 06 과 일치).
  · **부동산시장의 평가항목은 CD금리가 아니라 「주택담보대출금리」** 다.
    **= CD(91일) + 가산금리 1.57%.** 골든 검산: 3.12+1.57=4.69% → 4.2~4.8%미만 → 3점 보통 (캡쳐 09 와 일치).
  · 시설분류 원문: 상업시설(대형마트·백화점) · 의료시설(**종합병원**) · 공원 · 문화시설(극장·공연·전시장) ·
    공공시설(시·군·구청사·도서관). 이 앱은 사용자 확정에 따라 의료시설을 **병원급 이상**으로 넓게 본다(더 보수적).
  · 미반영(범위 밖): **규모및배치**(총세대수×0.5+용적률×0.25+건폐율×0.25) · **평형구성**(가중치 1.73/3.41/6.66) ·
    **인근아파트 초기분양률**(10/8/6/4/2) · **수용·환지 사업지구 특례**(지구면적별 하한 등급) — 전부 수기입력 시트다.

- **초기예상분양률 = 평가표의 결론**(2026-09-16). 급간표 수령.
  **이 앱이 채우는 모든 시트가 결국 이 한 숫자를 내기 위한 근거다** —
  가이드북 캡쳐 머리글이 「분양률 산정을 위한 평가기준」, 실제 평가표 파일명이
  「3. (탄벌A지구) **초기분양률 산정근거**_최종.xlsx」 다.
  ```
  종합평가 점수 = 분양가격지수 제외 항목 점수(A) + 분양가경쟁력 점수
  95↑ 100% · 85~95 90% · 75~85 80% · 65~75 70% · 55~65 60% · 45~55 50% · 35~45 40% · 35미만 30%
  오피스텔·도시형생활주택은 같은 점수에서 **한 칸씩 낮다**(정확히 10%p)
  100세대 미만 사업장은 **60% 상한**(전지역 해당)
  ```
  골든 검산: A 56 + 분양가경쟁력 9 = **65점 → 70%**.
  **같은 "초기분양률" 이 두 곳에 나온다 — 섞지 말 것.**
  · 「인근아파트 초기 분양률(10)」 = **입력 항목**. 옆 단지를 조사해 매기는 점수.
  · 「초기예상분양률(%)」 = **산정 결과**. 종합평가 점수에서 나오는 본건의 예상 분양률.
  **A 는 이 앱이 계산하지 않는다** — 자동수집 못 하는 항목(규모및배치·평형구성·인근초기분양률)이
  A 안에 들어 있다. 비교사업장 탭의 [본건 제원] 에 내부망 평가표 값을 입력받고,
  이 앱이 스스로 낸 항목 점수를 **대조표로 나란히** 보여준다.
  분양가경쟁력 산식은 **`src/lib/compare.js` 한 곳**에만 둔다 — 두 탭이 같은 함수를 써야 갈리지 않는다.

- **심사평점표 = 최종 산출물**(2026-09-16). 구조가 드러났다.
  ```
  시트별 항목 점수 → 종합평가 점수 → 초기예상분양률(%) → 초기분양률 배점(22)
                                                        → 종합평점 → 심사등급 → 보증료율
  ```
  **이 앱이 만든 초기예상분양률이 심사평점표의 「초기분양률(22)」 항목이 되어 최종 평점에 들어간다.**
  · **전체 구간표 수령(2026-09-16)** — 7개 항목 전부 값만 넣으면 점수가 난다.
    ```
    사업수익률(20)     13.5↑20 · 11.5↑18 · 9.5↑16 · 6.5↑14 · 3.5↑12 · 3.5미만 10
    초기분양률(22)     100%=22 · 90↑21 · 80↑19 · 70↑16 · 60↑13 · 50↑10 · 50미만 0
    누적DSCR(3)        1.05↑3 · 1.04↑2.8 · 1.03↑2.6 · 1.02↑2.4 · 1.01↑2.2 · 1.00↑2
    자기자금(20)       8%↑20 · 6%↑17 · 4.5%↑14 · 3%↑11 · 2.5%↑8 · 1%↑5   (1% 미만은 원문에 없다)
    시공순위(15)       1~20위 15 · 21~50 14 · 51~100 12 · 101~200 10 · 201~500 8 · 501~700 6 · 700초과 4
    신용등급(15)       AAA,AA 15 · A+ 14 · A- 12 · BBB+ 10 · BBB- 8 · BB+ 7   (중간등급은 표에 없다)
    PF보증잔액비율(5)  50%미만 5 · 100%미만 4.5 · 150%미만 4 · 200%미만 3.5 · 300%미만 3 · 300%이상 2
    감점               사고사망만인율 1점 (0.5배 초과 ~ 1.0배 이하)
    종합평점→등급·요율  86↑ 1등급 0.563% · 80~85 2등급 0.697% · 73~79 3등급 0.848%
                       66~72 4등급 0.982% · 60~65 5등급 1.104% · 60미만 보증거절
    ```
    **누적DSCR·PF보증잔액비율은 소수 점수가 있다**(2.8 / 4.5 …) — 정수로 반올림하지 말 것.
    **골든 재현(탄벌A지구)** : 10.64%→16 · 70%→16 · 1.05→3 · 4.51%→14 · 17위→15 · AAA→15 · 45.6%→5
    = **합계 84** · 감점 1 → **종합평점 83 → 2등급 → 0.697%**. 항목별로 전부 일치한다.
    (전에 "재현 합계 94점" 이라 적었던 것은 **틀렸다** — 최고점을 가정해 넣은 값이었다.)
  · **0점 처리 규칙을 자동으로 건다** — 초기분양률 50% 미만 **또는** 누적DSCR 1.00 미만이면
    **두 항목 평점을 모두 0점**. 실무자가 잊기 쉬운 연동이라 화면이 대신 걸되 배지·배너로 남긴다.
  · **사업성·시공자 항목은 값을 받는다** — 점수가 아니라 원시값(%, 순위, 등급)을 넣으면 구간표가 점수를 낸다.
    그 값들은 사업수지표·신용평가에서 나오므로 이 앱이 수집하지는 않는다(사용자 결정 2026-09-16).
  · 사업수익률의 **분양가 = Min(적정분양가, 예정분양가)** — 적정분양가를 이 앱의 비교사업장 탭이 낸다.
    공사비 = Max(공사비 최저기준, 도급계약서상 공사비).
  · **배점 판독 주의** : 합계 100 · 사업수지분석 65 · 시공자 15/15/5(=35) 다.
    캡쳐의 그룹 라벨 「시공자 사업수행능력 (30)」 은 항목 합·총계와 맞지 않는다 → **항목 합**을 쓴다.
  · **공동시공**(시공자의 모회사[상법 §342조의2]가 책임준공 약정하면 모회사를 공동시공자로 본다) 사업은
    **시공자별로 평점을 각각 내어 높은 쪽**을 적용한다 — 이 앱은 한 번에 한 시공자만 계산한다.

- **화면 직접 사용 점검**(2026-09-16). 배포본을 헤드리스 브라우저로 사람처럼 끝까지 조작해
  (주소확정 → 통계 → 시설 3종 → 비교사업장 → 수기입력 → 초기예상분양률 → 심사평점표 → 보관 → 복원)
  버그·동선 문제 **13건**을 찾아 고쳤다. 사슬 전체가 화면에서 도는 것을 처음 확인했다 —
  A 56 + 분양가경쟁력 6 = 62 → 60% → 배점 13 → 합계 81 → 감점 1 → 80 → 2등급 0.697%.
  검증기록: `docs/QA-화면사용점검.md`
  · 이 앱이 **이미 수집한 값을 다시 치게 하지 않는다** — 시공순위는 [17위 넣기]·[50위 이내 넣기] 로 한 번에.
    다만 **말없이 채우지는 않는다**(공동시공은 다른 시공자로 볼 때가 있어 자동채택이 늘 옳지 않다).
  · 못 누르는 버튼은 **사유를 옆에 적는다** — `title` 에만 적으면 마우스를 올리기 전엔 안 보인다.
  · **판정 안 한 것과 판정해서 나쁜 것을 구분한다**(2026-09-17). 교통환경·주거편의는 항목마다 따로
    판정해 평균으로 등급을 내므로, 판정 안 한 항목이 섞이면 평균이 통째로 틀어진다.
    수집 전 시트는 `pending`, 판정 전 기본점수는 노란 확정색에서 빼고 A 합산표까지 사유를 들고 간다.

- **화면 동선 정리**(2026-09-17). 1440x900 에서 스크롤 0 화면을 단계마다 캡쳐해 보고 고치기를 4회 반복.
  **원칙: 그 단계의 도구는 그 단계에서만 편다.** 주소 확정 후 입력폼은 한 줄 요약으로 접고,
  단계 버튼 줄·경계 지도·저장목록·통계카드·원천배지는 자료수집 단계 탭에서만 보인다.
  [이 조회 보관]·[엑셀 다운로드] 는 요약 줄 오른쪽에 붙어 어느 단계에서나 한 자리에 있다.
  · 실측: 심사평점표 표 첫 행 y **830 → 480px** · [주소 확정] 되돌아가기 **950 → 90px**
    · 수기입력 입력↔결과 세로차 **2px**
  · **과잉 실시간 반영** — 한 칸만 넣어도 합계에 16 이 떠 배점 100 옆에서 "16/100" 으로 읽혔다.
    합계 자리는 **진행도(1 / 7)**, 부분합은 근거 칸 괄호로. 숫자를 없앤 게 아니라 자리를 옮긴 것.
  · **클릭수는 최소화가 아니라 최적화** — 시설 수집을 [반경시설 수집 (3종)] 하나로 묶되(4→2클릭)
    **개별 재수집 버튼은 다 끝난 뒤 보조로 남긴다**(하나만 다시 받고 싶을 때가 있다).
  · 막힌 자리에는 **그 탭으로 가는 버튼**을 둔다 — "○○ 탭에서 하세요" 를 글로만 적으면 찾아가야 한다.
  검증기록: `docs/QA-화면동선.md`

- **실사례 심사 1건 완주**(2026-09-17). 답을 모르는 실제 사업장으로 사슬 전체를 돌렸다 —
  화서역 호수공원 아너스빌(수원 권선구 서둔동 212-1 · 삼환기업㈜ 76위 · 476세대 · 분양개시 2026-10-12).
  본건 예정분양가 7,677,759원/㎡(공급면적) · 비교사업장 1곳 평균 8,096,024 → 지수 94.83 → 분양가경쟁력 15
  · A 60 → 종합 75 → **초기예상분양률 80% → 배점 19 → 합계 77 → 3등급 0.848%**.
  이 과정에서 시트 점수 미표시·증빙 URL 키 노출 두 건을 찾아 고쳤다.
  검증기록: `docs/QA-실사례심사-화서역아너스빌.md`
  **공개 원천으로 못 얻는 것이 무엇인지도 그 문서가 목록으로 남긴다** —
  용적률·건폐율(사업계획승인서) · 인근초기분양률 · 인구유입요인 · 사업수지표·신용평가 항목.

- **실사례 검증 2건째 완주**(2026-09-23). 답을 모르는 실제 사업장으로 두 수정을 확인했다 —
  상동역 롯데캐슬 시그니처(부천 상동 540-1 · 롯데건설(주) · 1,859세대 · 분양개시 2026-09-14).
  · **통계 최신월 분리가 실제로 갈린다** — 조회월 202607 인데 KB 는 202608 을 쓴다.
    「더 최신이 있는데 안 쓰나」를 원천에 물어 확인했다(소비심리 202608 · 주택보급률 2025 는 **원천에 없다**).
  · **도로가 0m · 8m 로 잡힌다**(전에는 균일 120m 격자라 120m 가 하한). ± 가 25/100/150 으로 갈리고
    기준 문구가 「사업지 경계에서」 로 나온다. 노란 1km 선 안쪽에 975m 핀, 바깥에 1108m 핀 — 지도와 배지가 맞는다.
  · 곁들여 재현 확인 — **현대백화점 중동점 442m**(45건 상한 함정) · **순천향대부천병원 「상급종합」 784m**(종별 함정)
  · 이 과정에서 제외 문구 결함 1건을 찾아 고쳤다(위 함정 표).
  검증기록: `docs/QA-실사례검증-부천상동.md`

- **UI/UX 3시나리오 점검**(2026-09-24). 가상 사업성으로 사슬 전체를 세 번 돌리며
  **단계마다 화면의 버튼을 전수 기록**(라벨·비활성·주버튼·좌표)해 문제 9건을 찾아 고쳤다 —
  부천 상동 1,859세대(1등급 0.563%) · 여주 홍문동 696세대(5등급 1.104%) ·
  평택 평택동 **99세대**(0점 처리 → 보증거절).
  · 색 문법(고른 상태 vs 실행) · 완료 표시(탭 점·수집 버튼) · 막다른 길(0건 안내) ·
    조건 발동 표시(60% 상한) · 같은 글자 버튼(지도 ＋ vs 차선 ＋) · 넣는 버튼 자리 ·
    구 단위 색인 · 게이트웨이 오류 문구 · 그리기 중 손잡이 문구
  · **중복 트리거는 실제로 하나뿐이었다** — 보관/엑셀 두 벌은 `panelOpen` 으로 갈려 동시에 안 보이고,
    비교사업장 `[50위 이내]` 와 심사평점표 `[7위 넣기]` 는 **다른 항목**이다(자리만 맞췄다).
  검증기록: `docs/QA-UIUX-3시나리오.md`

## 미결 사항

- **평가기준 구간표를 전부 받았다**(2026-09-17 마지막 2개 수령). `config/scoring.json` 에만 둔다.
  ```
  지역경쟁력(5)    0.6%↑5 · 0.3~0.6 4 · 0.1~0.3 3 · -0.06~0.1 2 · -0.06미만 1   (골든 0.341% → 4점)
  소비심리지수(15)  130↑15 · 110~130 12 · 90~110 9 · 70~90 6 · 70미만 3         (골든 125.1 → 12점)
  지역미분양(15)    0.1%미만 15 · 0.1~0.25 12 · 0.25~0.5 9 · 0.5~1 6 · 1%↑ 3
                   산식 = 해당지역 (미분양주택수 ÷ 주민등록세대수) × 100
                   골든 93 ÷ 178,187 × 100 = 0.05% → 15점 매우양호
                   미분양주택수=통계누리 · 주민등록세대수=국가통계포털, 최근월 시·군·구
  지역수요(5)      주택보급률과 인구유입요인을 **각각 5점 척도로 매겨 평균** → 등급 → 대표점수
                   주택보급률  99%미만 5 · 99~104 4 · 104~109 3 · 109~113 2 · 113%↑ 1
                               (**낮을수록 높은 점수다** — 집이 모자란 곳이 수요가 있다)
                               광역시↑는 구, 시는 시, 시급미만은 군 단위. 미고시면 상급 행정구역
                   인구유입요인 2개↑ 5 · 1개 3 · 없음 1  (**4점·2점 행은 원문에 없다**)
                               신도시·혁신도시·기업도시·산업단지 등. **원천이 없어 사람이 센다**
                   골든 검산: 99.4% → 4점 · 요인 없음 → 1점 · (4+1)/2 = 2.5 → 보통 → **3점**
  ```
  A 의 구성: **자동 9개** · 값→점수 3개 · 점수 직접 **0개**.
  `PENDING_ITEMS` 가 비어 「구간표 미수령 항목」 칸 자체가 화면에서 사라진다(새 항목이 생기면 다시 나온다).
  규칙은 점수 높은 것부터 적고 위에서부터 먼저 맞는 것을 택한다 → "동시만족이면 최고점" 이 자동.
  아무것도 안 맞으면 `base` (사용자 확정: **기본 1점은 무조건 있다**).
  **교차검산**: 골든 A=56 인데 이 앱이 내는 10개 합이 52 다 → 규모및배치+평형구성 = 4.
  둘 다 배점 5 이므로 모순이 없다(전에는 두 항목이 미상이라 이 검산 자체가 불가능했다).

- **인근초기분양률 시트는 범위에서 제외**(사용자 결정).
  범위에 넣더라도 **비교사업장 목록을 돌려 쓰면 안 된다** — 선정기준이 다르다.
  대조표: `docs/규정-인근단지-선정기준.md`
- 6차선 왕복도로 **자동**판정. 현재는 **도로명 후보 자동추출 + 로드뷰 육안 판정**.
  · `/api/roads?x=&y=&radius=` — 사업지 둘레를 **격자**로 훑어(간격 = 반경/4, 50~120m)
    각 점의 도로명주소를 물어 도로명을 모은다. 도로는 POI 가 아니라 검색이 안 되기 때문이다.
    **고리(반경×방향) 표본은 쓰지 말 것** — 300m 고리에서 간격이 157m 라 큰 도로를 통째로 놓친다
    (송도 랜드마크로가 그렇게 빠졌다).
  · 도로명 접미사로 차로수를 **단정할 수 없다**(법 원문 확인):
    시행령 §3 은 "폭 40m↑ **이거나** 왕복 8차로↑" 처럼 **OR** 조건이고,
    §8②1 **단서**가 "대로와 로 또는 로와 길을 서로 바꾸어 사용할 수 있다" 고 열어두었으며,
    도로명은 구간 **설정 시점** 기준이라 이후 확장돼도 개명(§12)은 별도다.
    → 후보를 추리는 신호로만 쓰고 확정은 로드뷰로.
  · 법령 원문은 `law.go.kr/DRF/lawService.do?OC=test&target=law&MST=&type=JSON` 로 받을 수 있다
    (키 없이 `OC=test` 로 됨).
  · **수기 입력칸은 없앴다.** 후보에서 고르고(× 로 상관없는 도로 제거) 로드뷰로 센 차선 수만 넣으면
    이름·거리·점수가 평가표에 자동으로 찬다.
    도로를 바꾸면 차선 수는 0으로 되돌린다(앞 도로 값을 물려받으면 판정이 틀린다).
  · **고르는 것과 적용하는 것을 나눈다.** 행을 누르면 로드뷰만 그쪽으로 가고,
    [이 도로로 적용] 을 눌러야 평가표·지도에 박힌다. 적용된 행에는 "적용됨" 배지가 붙는다.
  · **큰 도로(대로·로)는 자동으로 지도에 찍는다.** 길·번길까지 찍으면 핀에 덮여 못 읽는다.
    적용한 도로가 맨 앞(파란 핀)에 온다. 몇 곳을 찍고 있는지 후보 목록 머리에 표시한다.
    로드뷰는 적용한 도로(없으면 가장 큰·가까운 후보)에서 자동으로 열린다.
  · 구간표를 받았다 — **100m/300m/500m/1km = 5/4/3/2점, 그 밖은 1점.**
    그래서 탐색 반경이 300m 가 아니라 **1km** 다(300m 만 보면 2~3점 구간을 통째로 놓친다).
    후보 목록에 "6차선이면 N점" 을 미리 보여준다.
  · 직접적인 답은 **도로명주소 전자지도 도로구간(SHP)의 차로수 속성** (data.go.kr/data/15050413).
    파일 배포라 전처리·적재가 필요하다.
  · 도시계획시설(도로) 폭원(광로/대로/중로 N류)은 **간접 추정**이다 — 중앙분리대·보도를
    포함한 폭이라 차선 수를 단정할 수 없다. 보조 근거로만 쓸 것.
- **오피스텔·도시형 급간의 맨 아랫줄**(종합 35점 미만 = 20%)은 원문에서 읽은 값이 아니라
  「한 칸씩(10%p) 낮다」 규칙으로 만든 값이다. 주택 최하단이 30% 라 한 칸 낮추면 20% 인데
  **원문 표가 30% 에서 끝날 수도 있다**(공원·문화·공공의 "3개 모두 존재" 행처럼 표가 그냥 끝난 전례가 있다).
  아파트만 다루면 안 걸린다 — 오피스텔·도시형을 고르면 화면이 그 사실을 말한다.
  자가진단의 급간 전수검사에서도 그 줄만 뺐다(추정값을 정답으로 감시하면 안 된다).
- CD금리 과거 소급조회 (필요해지면 ECOS 721Y001/2010000 로 백필).

## 작업 원칙

1. **분석 → 설계 → 개발 → 검증 → 피드백** 순서를 지킨다.
2. 추측한 코드/ID 를 config 에 넣지 않는다. 넣으려면 골든값으로 실제 검증하고 `verified: true` 를 단다.
3. 커밋 전 `npx next build` 로 컴파일 확인. 푸시 후 `/api/collect` 로 실제 검증.
4. 사용자에게 터미널 명령을 시키지 않는다.
5. **"찾았다" 와 "실제로 된다" 를 구분해서 말한다.** 매핑을 찾은 것과 호출이 성공한 것은 다르다.
6. 원천이 이상한 값을 주면 **캡쳐(정답지)와 대조해 해석을 의심한다.**
   의료시설 15건은 버그가 아니라 "의원급은 의료시설이 아니다" 라는 신호였다.
